spring: 
  profiles:
    active:
      -dev

---

spring:
  profiles: dev
  application:
    name: springcloud-config-yellowbean-dev

---

spring:
  profiles: test
  application:
    name: springcloud-config-yellowbean-test